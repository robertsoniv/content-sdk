// CLI configuration utilities for TanStack Start
export interface TanstackCliConfig {
  projectName: string;
  template: string;
  sitecoreApiHost: string;
  sitecoreSiteName: string;
  sitecoreApiKey?: string;
}
